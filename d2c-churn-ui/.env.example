# Copy this file to .env and point it at your backend.
# Local FastAPI (uvicorn app.main:app --reload):
VITE_API_URL=http://localhost:8000

# Once deployed (e.g. on Render), swap in the live URL instead:
# VITE_API_URL=https://d2c-churn-api.onrender.com
